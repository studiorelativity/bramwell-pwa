# 05_shell gate run — 2026-09-05, deployed origin

Origin: https://bramwell.no.fail, serving assets index-xLImbsTp.js /
scroll-CQRiiRuq.js / index-IviUPO8n.css / selftest-HuiwFnoU.js — byte-identical
hashes to `npm run build` of main at 16dd19c (source unchanged since dfe2918
apart from docs and a type-only edit), so the dev-server probes below exercise
the deployed code. Instruments: `scripts/shot.mjs` (17 probes, 0 errors,
0 failures, on the dev build — the `window.bramwell` seam is stripped from
production) and two one-off CDP scripts in the session scratchpad (gate05.mjs,
gate05-offline.mjs). Every "works" claim below is an `elementFromPoint` hit at
the control's centre. Screenshots: scratchpad/prod-shots, dev-shots.

## Verified by instrument
- Cold start signed out → first-run card; Connect hit-tested at 1440 and 390;
  demo button present and disabled; FAB disabled; no reconnect pill. (prod)
- Signed out over a warm cache → no first-run; calendar renders the cached
  events read-only (3 bars); reconnect pill appears and hit-tests; FAB
  disabled. (prod, cache seeded via localStorage, not a real expired token)
- Settings persist across reload: snap 45 clicked through hit-test, reload,
  prefs carries snapStepDays 45. (dev)
- Sheet survives a background refresh: open, `ctl.invalidate()`, still open
  and visible; the held-repaint probe (form open + sheet) also passes. (dev)
- Colour rules: no shared colorId reachable (every other row's choice is a
  disabled option, at 4 and at 11 rows, all viewports/schemes); add is dead at
  11; delete is a two-click confirm and removes the row with zero requests to
  any Google host; rename keeps the key (label Job, name work); display-hex and
  Google-colour swatches both rendered at 12px on every row. (dev)
- Service worker: registers and activates on first load; the cache holds the 4
  hashed assets, /, manifest, both icons. (prod)
- Offline open: a second Chrome on the warm profile with DNS disabled opens
  the app from the SW cache, renders the cached event, FAB disabled, reconnect
  pill shown; an uncacheable request fails, proving the browser was offline.
  (prod)
- FAB vs the last row's Sunday at 390×844: FAB top 780, Sunday bottom 752, no
  overlap; the scroller's bottom padding is `64px + env(safe-area-inset-bottom)`
  and the FAB's offset is `16px + env(...)`, so the notch inset moves both by the
  same amount. Also passes at 1440 and 1920. (prod, no inset emulation)
- No runtime exceptions on any probe run.

## Yours, on the real thing
- Connect with your Google account: lands on today. Then let the token go
  stale (or revoke it) over a warm cache: read-only + pill, and the pill
  reconnects.
- Sign out from the sheet returns to first-run.
- Install as PWA on the phone; open it in airplane mode; attempt a write:
  it must fail visibly and roll back.
- After the next deploy (A/B/C merge), open the installed app online: the new
  build arrives with no cache-name bump.
- Every mood readable at arm's length, light and dark: the moods are Warm,
  Paper, Cool, Sage, Dusk.
- FAB clears the last Sunday on the notched phone, by eye.

## Not tested
- Real OAuth token expiry (the stale state was produced by a seeded cache).
- Safe-area insets (headless Chrome cannot emulate `env()`); reasoned from CSS.

## Phone pass (human on the iPhone, with instrumentation) — 2026-09-05
- **Layout ran off the screen in both views.** iOS auto-zoom on 12px form
  fields (1.33×, measured 1.32–1.4× from three screenshots). → C ticket 11.
- **Desktop tab needed a sign-out to see a phone-side write.** No foreground
  refetch; only range change triggers it. → C ticket 12, SPEC amended.
- **"Can't edit on the phone."** Instrumented on the real device via a
  throwaway `evlog` Pages preview: day open, event edit, Add after zoom, and
  Year all deliver touch → click. Two causes: the phone was on the Reconnect
  pill with a dimmed FAB (quiet renewal blocked by Safari ITP; OPEN.md), and
  the year view's two-tap rule was invisible (SPEC amended, A step 9).
- **Landscape address bar** is Safari's; judged in the installed PWA.
- Verified by the human after Reconnect: black FAB, day opens, Add works,
  month view works.
- Not yet verified: PWA install, offline write rollback, every mood at arm's
  length, second-tap-opens on the real site (assumed from the preview run).
